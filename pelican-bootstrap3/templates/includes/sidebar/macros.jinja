{% macro title(name, no_icon, icon='home', icon_fa_prefix='fas') -%}
  {%- if no_icon -%}
    {{ name }}
  {%- else -%}
    <i class="{{ icon_fa_prefix }} fa-{{ icon }} fa-lg"></i><span class="icon-label">{{ name }}</span>
  {%- endif -%}
{%- endmacro %}
